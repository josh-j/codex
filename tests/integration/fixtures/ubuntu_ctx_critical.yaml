---
ubuntu_ctx:
  system:
    hostname: ubuntu-db-01
    memory:
      used_pct: 99.0
      total_mb: 32768
    disks:
      - device: /dev/sda1
        mount: /
        used_pct: 96.0
      - device: /dev/sdb1
        mount: /data
        used_pct: 82.0
    services:
      failed_count: 2
      failed_list:
        - mysql.service
        - prometheus-node-exporter.service
    load_avg: 100.0
    uptime_days: 200
  updates:
    reboot_pending: true
