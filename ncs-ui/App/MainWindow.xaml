<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    Title="NCS UI"
    Height="900"
    Width="1440"
    MinHeight="760"
    MinWidth="1220"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    ResizeMode="CanResize"
    Background="#111217"
    FontFamily="Segoe UI">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
            CaptionHeight="0"
            CornerRadius="0"
            GlassFrameThickness="0"
            ResizeBorderThickness="6"
            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#181b1f" />
        <SolidColorBrush x:Key="SurfaceAltBrush" Color="#1e2228" />
        <SolidColorBrush x:Key="SurfaceRaisedBrush" Color="#242932" />
        <SolidColorBrush x:Key="AccentBrush" Color="#7fb069" />
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#1f3326" />
        <SolidColorBrush x:Key="WarnBrush" Color="#d6a24a" />
        <SolidColorBrush x:Key="WarnSoftBrush" Color="#352a19" />
        <SolidColorBrush x:Key="DangerBrush" Color="#f06478" />
        <SolidColorBrush x:Key="DangerSoftBrush" Color="#381e24" />
        <SolidColorBrush x:Key="InfoBrush" Color="#78a9ff" />
        <SolidColorBrush x:Key="InfoSoftBrush" Color="#182742" />
        <SolidColorBrush x:Key="BorderBrush" Color="#2c3038" />
        <SolidColorBrush x:Key="TextBrush" Color="#d8dce2" />
        <SolidColorBrush x:Key="TextMutedBrush" Color="#8e939c" />

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,4,0,10" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="#1e2228" />
            <Setter Property="Foreground" Value="#d8dce2" />
            <Setter Property="CaretBrush" Value="#d8dce2" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Margin" Value="0,4,0,10" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="#1e2228" />
            <Setter Property="Foreground" Value="#d8dce2" />
            <Setter Property="CaretBrush" Value="#d8dce2" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,4,0,10" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Background" Value="#1e2228" />
            <Setter Property="Foreground" Value="#d8dce2" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template" Value="{DynamicResource NcsComboBoxTemplate}" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,8" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="#10140f" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.94" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#1e2228" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        </Style>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="42" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="#171a20" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="Bold" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#20252d" />
                    <Setter Property="BorderBrush" Value="#3a4250" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#282f38" />
                    <Setter Property="BorderBrush" Value="#465162" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="StatusCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="0,0,0,16" />
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TabPanel Grid.Row="0" Margin="0,0,0,16" IsItemsHost="True" />
                            <ContentPresenter Grid.Row="1" ContentSource="SelectedContent" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" Background="#151920" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="16" Padding="14,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" ContentSource="Header" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource SurfaceAltBrush}" />
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#3c4654" />
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#3c4654" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SectionExpanderStyle" TargetType="Expander">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border Background="Transparent">
                            <StackPanel>
                                <ToggleButton x:Name="HeaderSite" Background="Transparent" BorderThickness="0" Foreground="{TemplateBinding Foreground}" Focusable="False" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                    <Border Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <ContentPresenter ContentSource="Header" />
                                            <Border Grid.Column="1" Width="28" Height="28" Background="#161920" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="14" VerticalAlignment="Center">
                                                <TextBlock x:Name="ExpanderGlyph" Text="+" Foreground="{StaticResource TextBrush}" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                        </Grid>
                                    </Border>
                                </ToggleButton>
                                <ContentPresenter x:Name="ExpandSite" Visibility="Collapsed" Margin="0,10,0,0" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible" />
                                <Setter TargetName="ExpanderGlyph" Property="Text" Value="-" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" CornerRadius="8" SnapsToDevicePixels="True">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#1a1f27" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#1d2834" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NcsScrollBarRepeatButtonStyle" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Background" Value="#151820" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{TemplateBinding Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NcsScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="Background" Value="#3d4654" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder" Background="{TemplateBinding Background}" CornerRadius="6" Margin="2" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#58657a" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="#78a9ff" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="12" />
            <Setter Property="Background" Value="#151820" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource NcsScrollBarRepeatButtonStyle}" Command="ScrollBar.PageUpCommand" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource NcsScrollBarThumbStyle}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource NcsScrollBarRepeatButtonStyle}" Command="ScrollBar.PageDownCommand" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Height" Value="12" />
                                <Setter Property="Width" Value="Auto" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ComboItemBorder" Background="{TemplateBinding Background}" CornerRadius="6" Margin="4,2">
                            <ContentPresenter Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ComboItemBorder" Property="Background" Value="#25303c" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ComboItemBorder" Property="Background" Value="#1f3326" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ComboBoxToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="ToggleBorder" Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}" BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}" BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}" CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="36" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="1" Background="#161920" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0" CornerRadius="0,8,8,0">
                                    <TextBlock Text="▾" Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <ControlTemplate x:Key="NcsComboBoxTemplate" TargetType="ComboBox">
            <Grid>
                <ToggleButton Name="ToggleButton" Style="{StaticResource ComboBoxToggleButtonStyle}" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Focusable="False" />
                <ContentPresenter Margin="10,5,40,5" VerticalAlignment="Center" HorizontalAlignment="Left" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" />
                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                    <Border Background="#171b21" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="6" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                        <ScrollViewer>
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                        </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>
        </ControlTemplate>
        <DataTemplate x:Key="PreflightItemTemplate">
            <Border Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border x:Name="StatusBadge" Width="52" Height="24" CornerRadius="12" Background="{StaticResource DangerSoftBrush}" VerticalAlignment="Top">
                        <TextBlock x:Name="StatusBadgeText" Text="FAIL" Foreground="{StaticResource DangerBrush}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                        <TextBlock Margin="0,4,0,0" Text="{Binding Message}" TextWrapping="Wrap" Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Passed}" Value="True">
                    <Setter TargetName="StatusBadge" Property="Background" Value="{StaticResource AccentSoftBrush}" />
                    <Setter TargetName="StatusBadgeText" Property="Text" Value="PASS" />
                    <Setter TargetName="StatusBadgeText" Property="Foreground" Value="{StaticResource AccentBrush}" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <DataTemplate x:Key="PathItemTemplate">
            <Border Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10">
                <TextBlock Text="{Binding}" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap" />
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Border Background="#0f1117" BorderBrush="#2c3038" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="64" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border x:Name="TitleBarDragRegion" Grid.Row="0" Background="#0d1015" BorderBrush="#22262d" BorderThickness="0,0,0,1">
                <Grid Margin="16,12,12,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="12" Height="12" Background="{StaticResource AccentBrush}" CornerRadius="6" Margin="0,0,10,0" />
                        <StackPanel>
                            <TextBlock x:Name="TitleBarTitleText" Text="NCS UI" Foreground="White" FontSize="14" FontWeight="Bold" />
                            <TextBlock x:Name="TitleBarSubtitleText" Text="Operate and configure" Foreground="{StaticResource TextMutedBrush}" FontSize="11" />
                        </StackPanel>
                    </StackPanel>
                    <Border Grid.Column="1" Margin="18,0,0,0" Background="{StaticResource SurfaceAltBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="16" Padding="12,6" VerticalAlignment="Center">
                        <TextBlock x:Name="SetupSummaryText" Text="Settings incomplete" Foreground="{StaticResource TextBrush}" FontSize="11" FontWeight="SemiBold" />
                    </Border>
                    <Border
                        x:Name="RunStateBorder"
                        Grid.Column="2"
                        Margin="10,0,0,0"
                        Background="#1e2228"
                        BorderBrush="#2c3038"
                        BorderThickness="1"
                        CornerRadius="16"
                        Padding="12,6"
                        VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="State" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock x:Name="RunStateText" Text="Idle" Foreground="{StaticResource TextBrush}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" />
                            <TextBlock x:Name="TitleBarRunStateText" Visibility="Collapsed" Text="Idle" />
                            <TextBlock x:Name="RunMetaText" Margin="8,0,0,0" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Text="No run yet." VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <WrapPanel Grid.Column="3" VerticalAlignment="Center" Margin="18,0,0,0">
                        <Button x:Name="MinimizeWindowButton" Content="-" Style="{StaticResource TitleBarButtonStyle}" />
                        <Button x:Name="MaximizeWindowButton" Content="□" Style="{StaticResource TitleBarButtonStyle}" />
                        <Button x:Name="CloseWindowButton" Content="X" Style="{StaticResource TitleBarButtonStyle}" />
                    </WrapPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <TabControl>
                        <TabItem Header="Operate">
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="420" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,20,0">
                                    <StackPanel>
                                        <Border Style="{StaticResource StatusCardStyle}">
                                            <StackPanel>
                                                <DockPanel Margin="0,0,0,10">
                                                    <TextBlock DockPanel.Dock="Left" Text="Preflight" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}" />
                                                    <Border x:Name="PreflightStateBadge" DockPanel.Dock="Right" Background="{StaticResource WarnSoftBrush}" CornerRadius="12" Padding="10,4">
                                                        <TextBlock x:Name="PreflightStateText" Text="Not Run" Foreground="{StaticResource WarnBrush}" FontWeight="SemiBold" />
                                                    </Border>
                                                </DockPanel>
                                                <TextBlock x:Name="PreflightSummaryText" Margin="0,0,0,12" Text="Preflight has not been run." Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" />
                                                <Button x:Name="PreflightButton" Content="Run Preflight" Width="140" />
                                                <ListBox x:Name="PreflightListBox" Margin="0,10,0,0" BorderThickness="0" Background="{StaticResource PanelBrush}" Foreground="{StaticResource TextBrush}" ItemTemplate="{StaticResource PreflightItemTemplate}" />
                                            </StackPanel>
                                        </Border>

                                        <Border Style="{StaticResource StatusCardStyle}">
                                            <StackPanel>
                                                <TextBlock Text="Action" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}" />
                                                <TextBlock x:Name="ActionSummaryText" Margin="0,10,0,6" Text="Run across inventory." Foreground="White" FontSize="18" FontWeight="Bold" TextWrapping="Wrap" />
                                                <TextBlock x:Name="ScopeRequirementText" Margin="0,0,0,12" Foreground="{StaticResource InfoBrush}" Text="No extra scope required." TextWrapping="Wrap" />
                                                <ComboBox x:Name="ActionComboBox" />
                                                <TextBlock Text="Site" Foreground="{StaticResource TextMutedBrush}" />
                                                <TextBox x:Name="SiteTextBox" />
                                                <TextBlock Text="Host" Foreground="{StaticResource TextMutedBrush}" />
                                                <TextBox x:Name="HostTextBox" />
                                                <Expander IsExpanded="False" Style="{StaticResource SectionExpanderStyle}">
                                                    <Expander.Header>
                                                        <TextBlock Text="Advanced" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                                                    </Expander.Header>
                                                    <StackPanel Margin="0,14,0,0">
                                                        <TextBlock Text="Extra Args" Foreground="{StaticResource TextMutedBrush}" />
                                                        <TextBox x:Name="ExtraArgsTextBox" TextWrapping="Wrap" AcceptsReturn="True" Height="92" VerticalScrollBarVisibility="Auto" />
                                                    </StackPanel>
                                                </Expander>
                                                <WrapPanel Margin="0,12,0,0">
                                                    <Button x:Name="RunButton" Content="Run" />
                                                    <Button x:Name="CancelButton" Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" />
                                                </WrapPanel>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </ScrollViewer>

                                <StackPanel Grid.Column="1">
                                    <Border Style="{StaticResource StatusCardStyle}" Margin="0,0,0,16">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="Preview" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="1" x:Name="CommandReadinessText" Foreground="{StaticResource TextMutedBrush}" Text="Live preview" VerticalAlignment="Center" />
                                            </Grid>
                                            <TextBox x:Name="CommandPreviewTextBox" IsReadOnly="True" Height="90" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />
                                            <Expander IsExpanded="False" Style="{StaticResource SectionExpanderStyle}" Margin="0,12,0,0">
                                                <Expander.Header>
                                                    <TextBlock Text="Detected Paths" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                                                </Expander.Header>
                                                <StackPanel Margin="0,14,0,0">
                                                    <ListBox x:Name="DetectedPathsListBox" BorderThickness="0" Background="{StaticResource PanelBrush}" Foreground="{StaticResource TextBrush}" ItemTemplate="{StaticResource PathItemTemplate}" />
                                                </StackPanel>
                                            </Expander>
                                            <WrapPanel Margin="0,12,0,0">
                                                <Button x:Name="CopyOutputButton" Content="Copy Output" Style="{StaticResource SecondaryButtonStyle}" />
                                                <Button x:Name="ExportOutputButton" Content="Export Output" Style="{StaticResource SecondaryButtonStyle}" />
                                            </WrapPanel>
                                        </StackPanel>
                                    </Border>

                                    <Border Style="{StaticResource StatusCardStyle}" Margin="0">
                                        <Expander IsExpanded="False" Style="{StaticResource SectionExpanderStyle}" Margin="0">
                                            <Expander.Header>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="Console" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" />
                                                    <TextBlock Grid.Column="1" x:Name="ConsoleHintText" Foreground="{StaticResource TextMutedBrush}" Text="Newest at bottom" VerticalAlignment="Center" />
                                                </Grid>
                                            </Expander.Header>
                                            <TextBox
                                                x:Name="ConsoleTextBox"
                                                Margin="0,14,0,0"
                                                FontFamily="Consolas"
                                                FontSize="13"
                                                IsReadOnly="True"
                                                AcceptsReturn="True"
                                                TextWrapping="Wrap"
                                                VerticalScrollBarVisibility="Auto"
                                                HorizontalScrollBarVisibility="Auto"
                                                Background="#101319"
                                                Foreground="#d8dce2"
                                                CaretBrush="#d8dce2"
                                                BorderThickness="0" />
                                        </Expander>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </TabItem>

                        <TabItem Header="Settings">
                            <ScrollViewer Margin="0,6,0,0" VerticalScrollBarVisibility="Auto">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Style="{StaticResource StatusCardStyle}" Margin="0,0,10,16">
                                        <StackPanel>
                                            <TextBlock Text="Connection" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}" />
                                            <TextBlock Text="SSH Host" Foreground="{StaticResource TextMutedBrush}" Margin="0,12,0,0" />
                                            <TextBox x:Name="SshHostTextBox" />
                                            <TextBlock Text="SSH User" Foreground="{StaticResource TextMutedBrush}" />
                                            <TextBox x:Name="SshUserTextBox" />
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="140" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                                    <TextBlock Text="Port" Foreground="{StaticResource TextMutedBrush}" />
                                                    <TextBox x:Name="SshPortTextBox" />
                                                </StackPanel>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="Auth" Foreground="{StaticResource TextMutedBrush}" />
                                                    <ComboBox x:Name="SshAuthModeComboBox" />
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel x:Name="SshKeyPathPanel">
                                                <TextBlock Text="Key Path" Foreground="{StaticResource TextMutedBrush}" />
                                                <TextBox x:Name="SshKeyPathTextBox" />
                                            </StackPanel>
                                            <StackPanel x:Name="SshPasswordPanel">
                                                <TextBlock Text="Password" Foreground="{StaticResource TextMutedBrush}" />
                                                <PasswordBox x:Name="SshPasswordBox" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Style="{StaticResource StatusCardStyle}" Margin="10,0,0,16">
                                        <StackPanel>
                                            <TextBlock Text="Defaults" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextBrush}" />
                                            <TextBlock Text="Repo Path" Foreground="{StaticResource TextMutedBrush}" Margin="0,12,0,0" />
                                            <TextBox x:Name="RemoteRepoPathTextBox" />
                                            <TextBlock Text="Vault Path" Foreground="{StaticResource TextMutedBrush}" />
                                            <TextBox x:Name="RemoteVaultPathTextBox" />
                                            <TextBlock Text="Default Site" Foreground="{StaticResource TextMutedBrush}" />
                                            <TextBox x:Name="DefaultSiteTextBox" />
                                            <TextBlock Text="Default Host" Foreground="{StaticResource TextMutedBrush}" />
                                            <TextBox x:Name="DefaultHostTextBox" />
                                            <WrapPanel Margin="0,12,0,0">
                                                <Button x:Name="SaveSettingsButton" Content="Save Settings" />
                                            </WrapPanel>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </ScrollViewer>
                        </TabItem>
                    </TabControl>
                </Grid>

                <Border Grid.Row="2" Style="{StaticResource StatusCardStyle}" Margin="0,20,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="180" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Status" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" />
                            <TextBlock x:Name="StatusTextBlock" Text="Ready to configure." TextWrapping="Wrap" Foreground="{StaticResource TextBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Exit Code" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" />
                            <TextBlock x:Name="ExitCodeTextBlock" Text="-" Foreground="{StaticResource TextBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Duration" FontWeight="Bold" Foreground="{StaticResource TextMutedBrush}" />
                            <TextBlock x:Name="DurationTextBlock" Text="-" Foreground="{StaticResource TextBrush}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
