---
# playbooks/verify_atomic_refactor.yaml
- name: Verify Refactored Atomic Roles
  hosts: all
  gather_facts: false
  tasks:
    - name: 0) Verify New Audit Role
      ansible.builtin.include_role:
        name: internal.vmware.audit

    - name: 4) ESXi Deep Audits
      ansible.builtin.include_role:
        name: internal.vmware.stig
        tasks_from: esxi.yaml
      when: run_esxi_audit | default(false) | bool
