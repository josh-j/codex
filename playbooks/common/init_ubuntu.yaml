---
# playbooks/common/init_ubuntu.yaml
# Initialize context for Ubuntu system audits.

- name: Initialize Ubuntu Audit
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Initialize run context
      ansible.builtin.import_tasks: tasks/init_run_ctx.yaml
      vars:
        init_report_subdir: "Ubuntu_Ops"
