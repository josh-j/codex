---
# playbooks/common/init_infrastructure.yaml
# Initialize shared run context and scheduling metadata.

- name: Initialize Run Context
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Initialize run context
      ansible.builtin.import_tasks: tasks/init_run_ctx.yaml
      vars:
        init_enable_scheduling: true
