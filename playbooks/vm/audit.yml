---
# playbooks/vm/audit.yml
# Read-only audit focused on VM inventory and snapshot state.

- name: Phase 2c | VMware VM Workload Audit
  hosts: vcenters
  connection: local
  gather_facts: false
  roles:
    - role: internal.vmware.vm
      vars:
        ncs_action: collect
