# >>>
# is_read_only: true
# options:
#   windows_stig_native_checks_only: bool = false | Native Checks Only
#   windows_stig_target_type: select[windows_server_2022, windows_server_2019] = windows_server_2022 | STIG Profile
# <<<
---
# playbooks/windows/stig_audit.yml
# Security Technical Implementation Guide (STIG) audit for Windows Server.
# This playbook performs a detailed security assessment against DISA
# STIG requirements for Windows environments. It evaluates registry
# settings, Group Policy objects, user rights assignments, and service
# configurations. Findings are collected as raw telemetry for integration
# into the centralized security compliance dashboards.

- name: "Phase 3c: Windows Server STIG Audit"
  hosts: "{{ windows_target_hosts | default('windows_servers') }}"
  gather_facts: false
  vars:
    master_enable_windows_stig: true
  roles:
    - role: internal.windows.server
      vars:
        ncs_action: audit
        ncs_profile: stig
      when: master_enable_windows_stig | bool
