---
# inventory/production/group_vars/sites.yaml
# Site-specific connection parameters and endpoint patterns.

# Endpoint Naming Patterns
# These are used by roles to derive the target API endpoints for each site.
vmware_hostname: "vc-{{ inventory_hostname }}.local"
unity_endpoint: "https://unity-{{ inventory_hostname }}/api"
dd_endpoint: "https://dd-{{ inventory_hostname }}"
ucs_endpoint: "ucs-{{ inventory_hostname }}"
