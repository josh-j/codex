---
# Lab / test inventory.
# Used by internal.vmware.dev_esxi_standalone_stig_audit and
# internal.vmware.dev_vm_standalone_stig_audit for standalone ESXi/VM testing.
# Paired inventories live in inventory/lab/standalone/.

all:
  children:
    esxi_standalone:
      hosts: {}
        # lab-esxi01.yourdomain.mil:
        #   ansible_host: 10.0.0.50
        #   ansible_user: root
        #   ansible_password: changeme
        #   vmware_hostname: lab-esxi01.yourdomain.mil
        #   vmware_username: root
        #   vmware_password: changeme
        #   vmware_validate_certs: false
