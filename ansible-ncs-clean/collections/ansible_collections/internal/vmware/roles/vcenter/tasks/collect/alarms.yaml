---
# internal.vmware.vcenter : collect/alarms.yaml

- name: Alarms | Get triggered alarms via custom module
  internal.vmware.vmware_triggered_alarms_info:
  register: _raw_alarms
  delegate_to: localhost
  changed_when: false
