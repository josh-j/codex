---
# internal.vmware.vcenter_collect : defaults/main.yaml

# =============================================================================
# Connection Defaults
# =============================================================================
# vmware_username: "administrator@vsphere.local"
# vmware_password: ""
vmware_validate_certs: false

# =============================================================================
# Performance/Safety Toggles
# =============================================================================
vmware_discovery_timeout: 30
vmware_skip_discovery: false
vcsa_collect_appliance: true
vcsa_collect_inventory: true
vcsa_collect_alarms: true
vcsa_collect_datacenters: true
vcsa_collect_compute: true
vcsa_collect_storage: true
vcsa_collect_workload: true
vcsa_collect_snapshots: true

# =============================================================================
# Thresholds (Used by ncs-reporter)
# =============================================================================
vmware_cpu_critical_pct: 90.0
vmware_memory_critical_pct: 90.0
vmware_datastore_free_critical_pct: 10.0
vmware_datastore_free_warning_pct: 15.0
vmware_snapshot_age_warning_days: 7
vmware_snapshot_size_warning_gb: 100.0

# =============================================================================
# Simulation Settings
# =============================================================================
simulation_mode: false
simulation_vcsa_fixture_root: "{{ playbook_dir }}/../tests/reports/mock_production_run/platform/vmware/vcenter/vcsa"
simulation_vcsa_fixture_file: "{{ simulation_vcsa_fixture_root }}/{{ inventory_hostname }}/raw_vcenter.yaml"
