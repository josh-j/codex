---
# internal.compute.common : sync_ctx
# Standardizes context binding for Compute infrastructure audits.

- name: Sync Compute Context
  ansible.builtin.include_role:
    name: internal.core.common
    tasks_from: sync_ctx
  vars:
    ncs_domain_ctx_name: "compute_ctx"
