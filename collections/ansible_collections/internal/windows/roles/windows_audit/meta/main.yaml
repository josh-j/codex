---
# Role metadata for windows role
galaxy_info:
  author: Operations Team
  description: Windows Server checks and ConfigMgr updates
  company: Data Center Operations
  license: MIT
  min_ansible_version: "2.12"

  platforms:
    - name: Windows
      versions:
        - all

  galaxy_tags:
    - windows
    - server
    - applications
    - configmgr
    - updates

# Role dependencies
dependencies:
  - role: common

# Collections required by this role
collections:
  - ansible.builtin
  - ansible.windows
  - community.windows
