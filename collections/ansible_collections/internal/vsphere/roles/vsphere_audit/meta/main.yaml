---
# roles/vsphere/meta/main.yaml
# Role dependencies
dependencies:
  - role: internal.core.common

# Collections required by this role
collections:
  - ansible.builtin
  - community.vmware
  - community.general
