# internal.core.reporting

Task-library role for report assembly and rendering helpers.

Use this role via `ansible.builtin.include_role` with `tasks_from` (for example,
`prepare_platform_state.yaml` or `host_loop_render_template.yaml`) rather than
invoking the role without a task selector.
