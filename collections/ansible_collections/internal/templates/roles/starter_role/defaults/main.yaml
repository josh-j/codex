---
# Defaults for starter_role

# Skip flag for discovery (useful for slow APIs or known static environments)
starter_skip_discovery: false

# Platform name used in reporting and namespacing
starter_platform_name: "starter"

# Threshold for a sample check
starter_disk_threshold: 90
