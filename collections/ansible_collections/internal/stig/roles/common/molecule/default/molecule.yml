---
dependency:
  name: galaxy
driver:
  name: delegated
platforms:
  - name: localhost
provisioner:
  name: ansible
  inventory:
    links:
      hosts: ../../../../../../inventory/production/hosts # Optional: link to real inventory or keep internal
    host_vars:
      localhost:
        ansible_connection: local
verifier:
  name: ansible
