---
# Lab inventory skeleton for the internal.ios collection. Copy this
# tree to tests/inventory/ and edit. tests/inventory/ is gitignored.

all:
  children:
    ios_switches:
      hosts:
        sw01-lab:
          ansible_host: 10.0.0.10
