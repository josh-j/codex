---
# Deprecated. vCenter-wide VM/snapshot collection lives in the vcsa role
# now. Kept as a no-op so existing internal.vmware.vm_collect call sites
# (Justfile, ad-hoc playbooks) don't error; remove when they're retired.

- name: "VM Collect | Deprecated (no-op)"
  ansible.builtin.debug:
    msg: "internal.vmware.vm collect is deprecated; see internal.vmware.vcsa."
  changed_when: false
