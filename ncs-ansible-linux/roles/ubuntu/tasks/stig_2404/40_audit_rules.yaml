---
# =============================================================================
# Audit rules loaded via augenrules — migrated to internal.core.stig
# =============================================================================

- block:
    # --- usergroup_modification watches ---
    - name: "stigrule_270684"
      internal.core.stig:
        regexp: "^-w /etc/passwd -p wa -k usergroup_modification$"
        line: "-w /etc/passwd -p wa -k usergroup_modification"
      notify: reload_audit_rules

    - name: "stigrule_270685"
      internal.core.stig:
        regexp: "^-w /etc/group -p wa -k usergroup_modification$"
        line: "-w /etc/group -p wa -k usergroup_modification"
      notify: reload_audit_rules

    - name: "stigrule_270686"
      internal.core.stig:
        regexp: "^-w /etc/shadow -p wa -k usergroup_modification$"
        line: "-w /etc/shadow -p wa -k usergroup_modification"
      notify: reload_audit_rules

    - name: "stigrule_270687"
      internal.core.stig:
        regexp: "^-w /etc/gshadow -p wa -k usergroup_modification$"
        line: "-w /etc/gshadow -p wa -k usergroup_modification"
      notify: reload_audit_rules

    - name: "stigrule_270688"
      internal.core.stig:
        regexp: "^-w /etc/security/opasswd -p wa -k usergroup_modification$"
        line: "-w /etc/security/opasswd -p wa -k usergroup_modification"
      notify: reload_audit_rules

    # --- execve privilege escalation (270689 a-d) ---
    - name: "stigrule_270689a"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b64 -S execve -C uid!=euid -F euid=0 -F key=execpriv$"
        line: "-a always,exit -F arch=b64 -S execve -C uid!=euid -F euid=0 -F key=execpriv"
      notify: reload_audit_rules

    - name: "stigrule_270689b"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b64 -S execve -C gid!=egid -F egid=0 -F key=execpriv$"
        line: "-a always,exit -F arch=b64 -S execve -C gid!=egid -F egid=0 -F key=execpriv"
      notify: reload_audit_rules

    - name: "stigrule_270689c"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b32 -S execve -C uid!=euid -F euid=0 -F key=execpriv$"
        line: "-a always,exit -F arch=b32 -S execve -C uid!=euid -F euid=0 -F key=execpriv"
      notify: reload_audit_rules

    - name: "stigrule_270689d"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b32 -S execve -C gid!=egid -F egid=0 -F key=execpriv$"
        line: "-a always,exit -F arch=b32 -S execve -C gid!=egid -F egid=0 -F key=execpriv"
      notify: reload_audit_rules

    # --- journal / sudo log watches ---
    - name: "stigrule_270715"
      internal.core.stig:
        regexp: "^-w /var/log/journal -p wa -k systemd_journal$"
        line: "-w /var/log/journal -p wa -k systemd_journal"
      notify: reload_audit_rules

    - name: "stigrule_270740"
      internal.core.stig:
        regexp: '^-w /var/log/sudo\.log -p wa -k maintenance$'
        line: "-w /var/log/sudo.log -p wa -k maintenance"
      notify: reload_audit_rules

    # --- privileged command execution ---
    - name: "stigrule_270778"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/bin/su -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-priv_change$"
        line: "-a always,exit -F path=/bin/su -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-priv_change"
      notify: reload_audit_rules

    - name: "stigrule_270779"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/chfn -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-chfn$"
        line: "-a always,exit -F path=/usr/bin/chfn -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-chfn"
      notify: reload_audit_rules

    - name: "stigrule_270780"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/mount -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-mount$"
        line: "-a always,exit -F path=/usr/bin/mount -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-mount"
      notify: reload_audit_rules

    - name: "stigrule_270781"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/umount -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-umount$"
        line: "-a always,exit -F path=/usr/bin/umount -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-umount"
      notify: reload_audit_rules

    - name: "stigrule_270782"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/ssh-agent -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-ssh$"
        line: "-a always,exit -F path=/usr/bin/ssh-agent -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-ssh"
      notify: reload_audit_rules

    - name: "stigrule_270783"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/lib/openssh/ssh-keysign -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-ssh$"
        line: "-a always,exit -F path=/usr/lib/openssh/ssh-keysign -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-ssh"
      notify: reload_audit_rules

    # --- permission change syscalls (270786 a-b) ---
    - name: "stigrule_270786a"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b32 -S chmod,fchmod,fchmodat -F auid>=1000 -F auid!=-1 -k perm_chng$"
        line: "-a always,exit -F arch=b32 -S chmod,fchmod,fchmodat -F auid>=1000 -F auid!=-1 -k perm_chng"
      notify: reload_audit_rules

    - name: "stigrule_270786b"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b64 -S chmod,fchmod,fchmodat -F auid>=1000 -F auid!=-1 -k perm_chng$"
        line: "-a always,exit -F arch=b64 -S chmod,fchmod,fchmodat -F auid>=1000 -F auid!=-1 -k perm_chng"
      notify: reload_audit_rules

    # --- permission access syscalls (270787 a-d) ---
    - name: "stigrule_270787a"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b32 -S creat,open,openat,open_by_handle_at,truncate,ftruncate -F exit=-EPERM -F auid>=1000 -F auid!=-1 -k perm_access$"
        line: "-a always,exit -F arch=b32 -S creat,open,openat,open_by_handle_at,truncate,ftruncate -F exit=-EPERM -F auid>=1000 -F auid!=-1 -k perm_access"
      notify: reload_audit_rules

    - name: "stigrule_270787b"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b32 -S creat,open,openat,open_by_handle_at,truncate,ftruncate -F exit=-EACCES -F auid>=1000 -F auid!=-1 -k perm_access$"
        line: "-a always,exit -F arch=b32 -S creat,open,openat,open_by_handle_at,truncate,ftruncate -F exit=-EACCES -F auid>=1000 -F auid!=-1 -k perm_access"
      notify: reload_audit_rules

    - name: "stigrule_270787c"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b64 -S creat,open,openat,open_by_handle_at,truncate,ftruncate -F exit=-EPERM -F auid>=1000 -F auid!=-1 -k perm_access$"
        line: "-a always,exit -F arch=b64 -S creat,open,openat,open_by_handle_at,truncate,ftruncate -F exit=-EPERM -F auid>=1000 -F auid!=-1 -k perm_access"
      notify: reload_audit_rules

    - name: "stigrule_270787d"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b64 -S creat,open,openat,open_by_handle_at,truncate,ftruncate -F exit=-EACCES -F auid>=1000 -F auid!=-1 -k perm_access$"
        line: "-a always,exit -F arch=b64 -S creat,open,openat,open_by_handle_at,truncate,ftruncate -F exit=-EACCES -F auid>=1000 -F auid!=-1 -k perm_access"
      notify: reload_audit_rules

    # --- more privileged commands ---
    - name: "stigrule_270788"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/sudo -F perm=x -F auid>=1000 -F auid!=-1 -k priv_cmd$"
        line: "-a always,exit -F path=/usr/bin/sudo -F perm=x -F auid>=1000 -F auid!=-1 -k priv_cmd"
      notify: reload_audit_rules

    - name: "stigrule_270789"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/sudoedit -F perm=x -F auid>=1000 -F auid!=-1 -k priv_cmd$"
        line: "-a always,exit -F path=/usr/bin/sudoedit -F perm=x -F auid>=1000 -F auid!=-1 -k priv_cmd"
      notify: reload_audit_rules

    - name: "stigrule_270790"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/chsh -F perm=x -F auid>=1000 -F auid!=-1 -k priv_cmd$"
        line: "-a always,exit -F path=/usr/bin/chsh -F perm=x -F auid>=1000 -F auid!=-1 -k priv_cmd"
      notify: reload_audit_rules

    - name: "stigrule_270791"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/newgrp -F perm=x -F auid>=1000 -F auid!=-1 -k priv_cmd$"
        line: "-a always,exit -F path=/usr/bin/newgrp -F perm=x -F auid>=1000 -F auid!=-1 -k priv_cmd"
      notify: reload_audit_rules

    - name: "stigrule_270792"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/chcon -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng$"
        line: "-a always,exit -F path=/usr/bin/chcon -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng"
      notify: reload_audit_rules

    - name: "stigrule_270793"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/sbin/apparmor_parser -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng$"
        line: "-a always,exit -F path=/sbin/apparmor_parser -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng"
      notify: reload_audit_rules

    - name: "stigrule_270794"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/setfacl -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng$"
        line: "-a always,exit -F path=/usr/bin/setfacl -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng"
      notify: reload_audit_rules

    - name: "stigrule_270795"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/chacl -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng$"
        line: "-a always,exit -F path=/usr/bin/chacl -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng"
      notify: reload_audit_rules

    # --- privileged password/account management ---
    - name: "stigrule_270798"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/passwd -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-passwd$"
        line: "-a always,exit -F path=/usr/bin/passwd -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-passwd"
      notify: reload_audit_rules

    - name: "stigrule_270799"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/sbin/unix_update -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-unix-update$"
        line: "-a always,exit -F path=/sbin/unix_update -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-unix-update"
      notify: reload_audit_rules

    - name: "stigrule_270800"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/gpasswd -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-gpasswd$"
        line: "-a always,exit -F path=/usr/bin/gpasswd -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-gpasswd"
      notify: reload_audit_rules

    - name: "stigrule_270801"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/chage -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-chage$"
        line: "-a always,exit -F path=/usr/bin/chage -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-chage"
      notify: reload_audit_rules

    - name: "stigrule_270802"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/sbin/usermod -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-usermod$"
        line: "-a always,exit -F path=/usr/sbin/usermod -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-usermod"
      notify: reload_audit_rules

    - name: "stigrule_270803"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/crontab -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-crontab$"
        line: "-a always,exit -F path=/usr/bin/crontab -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-crontab"
      notify: reload_audit_rules

    - name: "stigrule_270804"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/sbin/pam_timestamp_check -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-pam_timestamp_check$"
        line: "-a always,exit -F path=/usr/sbin/pam_timestamp_check -F perm=x -F auid>=1000 -F auid!=-1 -k privileged-pam_timestamp_check"
      notify: reload_audit_rules

    # --- kernel module changes (270806 a-b) ---
    - name: "stigrule_270806a"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b32 -S delete_module -F auid>=1000 -F auid!=-1 -k module_chng$"
        line: "-a always,exit -F arch=b32 -S delete_module -F auid>=1000 -F auid!=-1 -k module_chng"
      notify: reload_audit_rules

    - name: "stigrule_270806b"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b64 -S delete_module -F auid>=1000 -F auid!=-1 -k module_chng$"
        line: "-a always,exit -F arch=b64 -S delete_module -F auid>=1000 -F auid!=-1 -k module_chng"
      notify: reload_audit_rules

    # --- sudoers watches ---
    - name: "stigrule_270807"
      internal.core.stig:
        regexp: "^-w /etc/sudoers -p wa -k privilege_modification$"
        line: "-w /etc/sudoers -p wa -k privilege_modification"
      notify: reload_audit_rules

    - name: "stigrule_270808"
      internal.core.stig:
        regexp: "^-w /etc/sudoers.d -p wa -k privilege_modification$"
        line: "-w /etc/sudoers.d -p wa -k privilege_modification"
      notify: reload_audit_rules

    # --- fdisk ---
    - name: "stigrule_270815"
      internal.core.stig:
        regexp: "^-w /usr/sbin/fdisk -p x -k fdisk$"
        line: "-w /usr/sbin/fdisk -p x -k fdisk"
      notify: reload_audit_rules

    # --- cron job watches (274870 a-b) ---
    - name: "stigrule_274870a"
      internal.core.stig:
        regexp: "^-w /etc/cron.d/ -p wa -k cronjobs$"
        line: "-w /etc/cron.d/ -p wa -k cronjobs"
      notify: reload_audit_rules

    - name: "stigrule_274870b"
      internal.core.stig:
        regexp: "^-w /var/spool/cron/ -p wa -k cronjobs$"
        line: "-w /var/spool/cron/ -p wa -k cronjobs"
      notify: reload_audit_rules

    # --- init_module (270796 a-b) ---
    - name: "stigrule_270796a"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b32 -S init_module -F auid>=1000 -F auid!=-1 -k module_chng$"
        line: "-a always,exit -F arch=b32 -S init_module -F auid>=1000 -F auid!=-1 -k module_chng"
      notify: reload_audit_rules

    - name: "stigrule_270796b"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b64 -S init_module -F auid>=1000 -F auid!=-1 -k module_chng$"
        line: "-a always,exit -F arch=b64 -S init_module -F auid>=1000 -F auid!=-1 -k module_chng"
      notify: reload_audit_rules

    # --- finit_module (270797 a-b) ---
    - name: "stigrule_270797a"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b32 -S finit_module -F auid>=1000 -F auid!=-1 -k module_chng$"
        line: "-a always,exit -F arch=b32 -S finit_module -F auid>=1000 -F auid!=-1 -k module_chng"
      notify: reload_audit_rules

    - name: "stigrule_270797b"
      internal.core.stig:
        regexp: "^-a always,exit -F arch=b64 -S finit_module -F auid>=1000 -F auid!=-1 -k module_chng$"
        line: "-a always,exit -F arch=b64 -S finit_module -F auid>=1000 -F auid!=-1 -k module_chng"
      notify: reload_audit_rules

    # --- login watches ---
    - name: "stigrule_270805"
      internal.core.stig:
        regexp: "^-w /var/log/lastlog -p wa -k logins$"
        line: "-w /var/log/lastlog -p wa -k logins"
      notify: reload_audit_rules

    - name: "stigrule_270810"
      internal.core.stig:
        regexp: "^-w /var/log/wtmp -p wa -k logins$"
        line: "-w /var/log/wtmp -p wa -k logins"
      notify: reload_audit_rules

    - name: "stigrule_270811"
      internal.core.stig:
        regexp: "^-w /var/log/btmp -p wa -k logins$"
        line: "-w /var/log/btmp -p wa -k logins"
      notify: reload_audit_rules

    - name: "stigrule_270812"
      internal.core.stig:
        regexp: "^-w /var/run/utmp -p wa -k logins$"
        line: "-w /var/run/utmp -p wa -k logins"
      notify: reload_audit_rules

    # --- 270813 - chcon (same rule as 270792, different V-number) ---
    - name: "stigrule_270813"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/usr/bin/chcon -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng$"
        line: "-a always,exit -F path=/usr/bin/chcon -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng"
      notify: reload_audit_rules

    # --- 270814 - apparmor_parser (same rule as 270793, different V-number) ---
    - name: "stigrule_270814"
      internal.core.stig:
        regexp: "^-a always,exit -F path=/sbin/apparmor_parser -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng$"
        line: "-a always,exit -F path=/sbin/apparmor_parser -F perm=x -F auid>=1000 -F auid!=-1 -k perm_chng"
      notify: reload_audit_rules

  module_defaults:
    internal.core.stig:
      _stig_apply: ansible.builtin.lineinfile
      _stig_notify: [reload_audit_rules]
      _stig_gate_packages: [auditd]
      _stig_gate_status: failed
      path: /etc/audit/rules.d/stig.rules
      create: true
