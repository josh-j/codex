---
# ubuntu2404.yaml
#
# This file is loaded via ``include_tasks: "{{ _ncs_stig_task_file }}"`` from
# ``internal.core.stig_orchestrator``, which means relative ``import_tasks``
# paths fall through to ``playbook_dir`` (not ``role_path``) and miss. Each
# step uses ``include_tasks`` with a templated absolute path under
# ``_ncs_task_root`` so resolution stays anchored to the role's tasks/ dir.

- name: "Ubuntu STIG | Prelude"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/00_prelude.yaml"

- name: "Ubuntu STIG | Packages and services"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/10_packages_services.yaml"

- name: "Ubuntu STIG | SSH crypto"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/20_ssh_crypto.yaml"

- name: "Ubuntu STIG | Kernel and boot hardening"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/30a_kernel_boot.yaml"

- name: "Ubuntu STIG | SSH and system services"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/30b_ssh_system.yaml"

- name: "Ubuntu STIG | System config audits"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/31_system_config_audits.yaml"
  when: _ubuntu2404_stig_phase == 'audit'

- name: "Ubuntu STIG | Accounts and PAM"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/30c_accounts_pam.yaml"

- name: "Ubuntu STIG | Access and PAM"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/32_access_pam.yaml"

- name: "Ubuntu STIG | Audit and logging"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/30d_audit_logging.yaml"

- name: "Ubuntu STIG | Filesystem, network, and PKI"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/33_fs_network_pki.yaml"

- name: "Ubuntu STIG | AIDE"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/34_aide.yaml"

- name: "Ubuntu STIG | Remaining filesystem and audit log controls"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/35_remaining_fs_audit.yaml"

- name: "Ubuntu STIG | Audit rules"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/40_audit_rules.yaml"

- name: "Ubuntu STIG | Audit rule flush"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/40a_audit_rule_flush.yaml"

- name: "Ubuntu STIG | Audit rule audits"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/41_audit_rule_audits.yaml"
  when: _ubuntu2404_stig_phase == 'audit'

- name: "Ubuntu STIG | Audit daemon and tooling"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/42_auditd_controls.yaml"

- name: "Ubuntu STIG | Post-STIG cleanup"
  ansible.builtin.include_tasks: "{{ _ncs_task_root }}/stig_2404/90_post_stig.yaml"
