---
# Password status check — delegates to shared Linux implementation.

- name: "Ubuntu | Password status"
  ansible.builtin.include_role:
    name: internal.linux.common
    tasks_from: password_status
  vars:
    _ncs_role_label: Ubuntu
