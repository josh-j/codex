---
# internal.ise.ise : defaults/main.yaml

# Cisco ISE API credentials. Override from inventory / group_vars / vault.
# Two API surfaces, two targets:
#   - PAN (ise_pan_hostname): ERS (/ers/...) + OpenAPI (/api/v1/...) — endpoints,
#     network devices, ANC policies, ANC endpoints, ANC PUT.
#   - MnT (ise_mnt_hostname): /admin/API/mnt/... — active sessions, auth history,
#     failure reasons, session-by-mac/username, CoA.
# Both fall back to ise_hostname for single-node deployments and legacy
# inventories that haven't been split yet.
ise_hostname: "{{ ansible_host | default(inventory_hostname) }}"
ise_pan_hostname: "{{ ise_hostname }}"
ise_mnt_hostname: "{{ ise_hostname }}"
ise_username: admin
ise_password: ""
ise_verify: false
ise_version: "3.5.0"
ise_wait_on_rate_limit: true
# false → SDK hits ERS on port 9060 directly (the path operator curl tests
# usually use); true → routes ERS through the OpenAPI gateway on 443. The
# gateway doesn't accept ERS-style filter=field.OP.value, so we default to
# the direct path. OpenAPI-only modules (system_certificate_info, etc.)
# still reach 443/api/v1/... regardless of this flag.
ise_uses_api_gateway: false
ise_uses_csrf_token: false
ise_debug: false
ise_single_request_timeout: 60

# Skip telemetry emission (useful for ad-hoc runs).
ise_skip_export: false

# Optional high-cardinality inventory sections. Keep endpoints off by
# default because large deployments can return a lot of data.
ise_collect_network_devices: true
ise_collect_endpoints: false
ise_collect_guests: false
ise_collect_trustsec: true
ise_collect_certificates: true
ise_collect_node_health: true
ise_collect_backup_repository: true
ise_collect_patches: true
ise_collect_policy: true
ise_collect_device_admin: true
ise_collect_identity_sources: true
ise_collect_sessions: true
ise_collect_active_session_list: false

# Optional lists for APIs that require a target. Leave empty to avoid
# guessing deployment-specific node or repository names.
ise_replication_nodes: []
ise_repository_file_names: []
ise_authentication_status_seconds: 3600
ise_authentication_status_records: 25
ise_endpoint_repeat_counter_threshold: 3
