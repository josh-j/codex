---
# internal.__COLLECTION_NAME__.example : meta/main.yaml
galaxy_info:
  role_name: example
  author: NCS
  description: Example role scaffold for the internal.__COLLECTION_NAME__ collection.
  license: GPL-3.0-or-later
  min_ansible_version: "2.15"
# `internal.core.dispatch` / `internal.core.emit` are invoked via
# explicit `include_role:` in tasks (so the per-call `vars:` block
# reaches them). Don't list them as meta deps — Ansible would
# auto-load them at role-include time without those vars and they'd
# fail rendering `_ncs_role_label`-templated task names.
dependencies: []
