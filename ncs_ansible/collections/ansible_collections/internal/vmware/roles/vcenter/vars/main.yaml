---
# internal.vmware.vcenter : vars/main.yaml
#
# INTERNAL CONTRACTS & SKELETONS
# Dynamically generated from schema to prevent duplication and drift.
vmware_ctx: "{{ 'internal.vmware:schemas/vmware_ctx.yaml' | internal.core.schema_to_skeleton('vmware_ctx') }}"
