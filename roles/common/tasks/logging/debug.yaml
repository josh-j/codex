---
- name: Log DEBUG message
  ansible.builtin.include_tasks: main.yaml # use _emit
  vars:
    ops_log_level: "debug"
  # Optimization: Only include the heavy logging task if debug is actually on
  when: ops.config.debug_mode | default(false) | bool
