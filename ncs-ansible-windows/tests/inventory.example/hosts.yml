---
# internal.windows test inventory skeleton.
# Copy to tests/inventory/ and replace with real lab hosts. The
# collect + STIG playbooks default to the windows_servers group.

all:
  children:
    windows_servers:
      hosts:
        win-lab-01.example.test:
          ansible_host: 10.0.2.10
