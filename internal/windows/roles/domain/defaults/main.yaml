---
# internal.windows.domain : defaults/main.yaml

# AD query defaults
domain_search_filter: "*"
domain_search_base: ""

# Stale account thresholds
domain_stale_computer_days: 90
domain_stale_user_days: 90

# DHCP (required for dhcp_scopes operation)
domain_dhcp_server: ""
