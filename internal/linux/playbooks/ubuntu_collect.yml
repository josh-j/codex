---
# internal/linux/playbooks/ubuntu_collect.yml
# Read-only Ubuntu discovery and health collection.

- name: "Ubuntu | Discover"
  hosts: "{{ ubuntu_target_hosts | default('ubuntu_servers') }}"
  gather_facts: false
  tags: ["ubuntu", "audit", "discover", "system"]
  roles:
    - role: internal.linux.ubuntu
      vars:
        ncs_action: collect
