---
# collections/ansible_collections/internal/vmware/roles/common/tasks/finalize_collection.yaml
#
# Shared collection finalization for VMware audit roles.

- name: Finalize | Assemble Raw Context
  ansible.builtin.include_tasks: "{{ vmware_collection_assemble_task }}"
  tags: [collect]

- name: Finalize | Export Raw State
  ansible.builtin.include_tasks: "{{ vmware_collection_export_task }}"
  tags: [collect]

