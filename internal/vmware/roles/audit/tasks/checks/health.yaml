---
# internal.vmware.audit : checks/health.yaml

- name: Audit | Evaluate vCenter Infrastructure Health
  ansible.builtin.set_fact:
    vmware_alerts: >-
      {{
        (vmware_alerts | default([]) | list)
        + (vmware_ctx | internal.vmware.audit_health_alerts)
      }}
  changed_when: false
