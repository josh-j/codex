---
# internal.template.example : tasks/stig_v1r0/main.yaml
# TEMPLATE: Add numbered include files as your STIG implementation grows.

- name: "Example STIG v1r0 | Run all checks"
  block:
    - name: Include prelude
      ansible.builtin.include_tasks: 00_prelude.yaml

    # TEMPLATE: Add numbered STIG task includes here:
    # - name: Include packages and services
    #   ansible.builtin.include_tasks: 10_packages_services.yaml
    #
    # - name: Include SSH and crypto
    #   ansible.builtin.include_tasks: 20_ssh_crypto.yaml

    - name: Include post-STIG
      ansible.builtin.include_tasks: 90_post_stig.yaml
