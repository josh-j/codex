# Imports the monorepo's shared recipes when the monorepo is
# alongside this repo; silently no-ops otherwise so the sibling works
# in isolation (no recipes available, but `just --list` still runs).

import? '../ncs-ansible/shared.just'
import? '../codex/shared.just'

# Collection-specific recipes below. See shared.just for
# lint / yamllint / build / clean.

# Run the collection's standalone smoke playbook against localhost.
# internal.core has no platform playbooks; smoke.yml exercises the
# stig action plugin end-to-end. See tests/README.md.
test:
    {{_ap}} -i {{_test_inv}} --vault-password-file {{_test_vault}} tests/smoke.yml

# Same as `test` but without --check — no-op here since smoke.yml does
# no mutations, kept for parity with the other collections.
test-apply: test