platform: linux/ubuntu
stig_playbook: "playbooks/ubuntu_stig_remediate.yml"
stig_target_var: "ubuntu_target_hosts"
stig_rule_prefixes:
  UBTU: ubuntu
  GEN: ubuntu


fleet_columns:
  - "Distribution: distribution"
  - "Uptime (d): uptime_days"
  - "Critical: _critical_count"
  - "Warning: _warning_count"

fields:
  $include: "linux_base_fields.yaml"
  collected_at: "raw_ubuntu.metadata.timestamp"

alerts:
  $include: "linux_base_alerts.yaml"

widgets:
  $include: "linux_base_widgets.yaml"
